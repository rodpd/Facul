#import <stdio.h>
int n = IO.readint ( );
string s = IO.readString ( );
int soma = 0;
for ( int i = 0; i < n; i = i + 2 )
{
int numero = n.charAt ( i );
soma = soma + numero;
}
saida = soma - n;